#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="$ROOT/target/release/gpu-monitor"

if [[ ! -x "$BIN" ]]; then
  echo "[gpu-monitor] Binary not found."
  echo "[gpu-monitor] Run: ./install.sh"
  exit 1
fi

exec "$BIN" "$@"
