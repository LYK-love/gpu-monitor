#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BIN="$ROOT/target/release/gpu-monitor"

if [[ ! -x "$BIN" ]]; then
  echo "[gpumon] Rust binary not found."
  echo "[gpumon] Run: $ROOT/install.sh"
  exit 1
fi

exec "$BIN" "$@"
